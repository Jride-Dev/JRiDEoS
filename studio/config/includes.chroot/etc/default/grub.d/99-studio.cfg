# Installed-system kernel cmdline for RT audio. (Live session gets the same
# flags via --bootappend-live in studio/auto/config.)
GRUB_CMDLINE_LINUX_DEFAULT="${GRUB_CMDLINE_LINUX_DEFAULT} threadirqs preempt=full intel_pstate=active i915.enable_guc=3"
